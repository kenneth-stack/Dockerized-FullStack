events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;
    error_log   /var/log/nginx/error.log warn;

    sendfile        on;
    keepalive_timeout  65;

    # Redirect all HTTP traffic to HTTPS
    server {
        listen 80;
        server_name delightdev.ng www.delightdev.ng db.delightdev.ng proxy.delightdev.ng;
        return 301 https://$host$request_uri;
    }

    server {
        listen 443 ssl;
        server_name www.delightdev.ng;
        ssl_certificate /etc/letsencrypt/live/delightdev.ng/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/delightdev.ng/privkey.pem;
        return 301 https://delightdev.ng$request_uri;
    }

    server {
        listen 443 ssl;
        server_name delightdev.ng;
        ssl_certificate /etc/letsencrypt/live/delightdev.ng/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/delightdev.ng/privkey.pem;

        location / {
            proxy_pass http://frontend:5000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /api {
            proxy_pass http://backend:8000;  # Adjusted port to match your backend configuration
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /docs {
            proxy_pass http://backend:8000/docs;  # Adjusted port to match your backend configuration
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name db.delightdev.ng;
        ssl_certificate /etc/letsencrypt/live/delightdev.ng/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/delightdev.ng/privkey.pem;

        location / {
            proxy_pass http://adminer:8080;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name proxy.delightdev.ng;
        ssl_certificate /etc/letsencrypt/live/delightdev.ng/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/delightdev.ng/privkey.pem;

        location / {
            proxy_pass http://nginx-proxy-manager:81;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
